services:
  web:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./src:/app/src
      - ./public:/app/public
    env_file:
      - .env
    # environment:
    #   - CHOKIDAR_USEPOLLING=true